# Original review

## VERDICT
**MAJOR_REVISION**

Проект демонстрирует глубокую теоретическую проработку и выдающуюся самодокументированность, однако содержит фундаментальные архитектурные противоречия, отсутствие тестового покрытия и низкую инженерную дисциплину. Кодовая база не соответствует заявленным принципам (static-first, Rust-first) и не готова к публичному запуску без серьёзной доработки.

---

## SCORES (1–5)

| Критерий | Оценка | Обоснование |
|----------|--------|-------------|
| **Architecture** | 3 | Хорошая концептуальная архитектура (тривиум, синкордия), но реализация размыта: статический фронтенд + непонятный Rust‑сервер + монолитный Python‑пайплайн. Нарушение принципа статичности. |
| **Optimality** | 2 | Загрузка 64 МБ сырых JSON в браузер, линейный поиск по 128k записям, генерация 9 с/слово, отсутствие индексов и кэширования – далеко от оптимального. |
| **Structure / Modularity** | 2 | `app.js` ~2500 строк без выделения логики; пайплайн не модульный; запланированные модули (syncordia.js, srs.js) отсутствуют. |
| **Systematicity** | 4 | Документация отличная, схемы данных согласованы, кросс-файловые ссылки корректны. Однако код часто расходится с документацией. |
| **Core‑files vs code alignment** | 3 | Концепты (тривиум, синкордия) частично реализованы, но ключевая подсистема Syncordia не закодирована, а Rust‑сервер не описан в core‑файлах. |
| **Stack‑rule compliance** (Rust+Phoenix only) | 1 | Фактический стек: Rust (4 файла), Python (5 скриптов), JS (1 файл). Phoenix отсутствует. Правило нарушено. |
| **Modernity of stack** | 2 | Vanilla JS (намеренно), espeak‑ng, устаревшие Python‑библиотеки. Отсутствуют TypeScript, WASM, GraphQL, CI, контейнеризация. |
| **Quality of processes / connections** | 2 | Нет тестов, нет CI, нет автоматизации сборки. Версионирование данных отсутствует. Коммуникация между компонентами только через файлы – это плюс, но без тестов ненадёжно. |

---

## CRITICAL ISSUES

1. **Наличие Rust‑сервера при декларируемом статическом подходе**  
   `server/` содержит `Cargo.toml`, `Dockerfile`, `src/`. Ни один из core‑документов не описывает назначение этого сервера. Если он не используется, он должен быть удалён или перемещён в `_archive`. Если используется – необходимо описать его роль, API и как он сочетается с static‑first архитектурой (п.1 DESIGN.md).  
   *Файлы*: `server/Cargo.toml`, `server/Dockerfile`.

2. **Полное отсутствие тестов**  
   Нет unit‑тестов, integration‑тестов, schema‑валидации. Пайплайн генерирует 127 645 записей без какой‑либо автоматической проверки корректности. Регрессия останется незамеченной до жалобы пользователя.  
   *Файл*: `OPEN_PROBLEMS.md §3.6` – проблема осознаётся, но не решена.

3. **Несоответствие заявленному стеку (Rust+Phoenix only)**  
   Весь пайплайн (генерация лексиконов, обогащение, сбор корпусов) написан на Python, что прямо противоречит правилу стека, установленному в задании. Исключение для Python разрешено только для существующих скриптов, но проект создаёт новые скрипты на Python.  
   *Файлы*: все `.py` в корне, `CLAUDE.md` §2.

4. **Критическая производительность фронтенда**  
   При загрузке страницы в память загружаются все 8 лексиконов (~64 МБ без сжатия). Поиск – линейный `O(n)` по неиндексированному массиву. Для мобильных устройств и медленных сетей время загрузки и время отклика неприемлемы.  
   *Файл*: `PARAMETERS.md` §7, `website/js/app.js`.

5. **Монолитный `app.js`**  
   Один файл содержит ~2500 строк, объединяя рендеринг, поиск, SRS, навигацию, работу с localStorage. Отсутствие модульности затрудняет поддержку и тестирование. Запланированные выделения (`srs.js`, `syncordia.js`) не выполнены.  
   *Файл*: `website/js/app.js`, `DESIGN.md` §4.4.

6. **Пайплайн не в `scripts/`**  
   Все Python‑скрипты находятся в корне репозитория, тогда как `DESIGN.md` §2 предписывает размещение в `scripts/`. Это приводит к захламлению корня и нарушает систематичность.  
   *Файлы*: `*.py` в корне, `DESIGN.md` §2.

---

## MINOR ISSUES

1. **Путь `WorkingDirectory` в systemd‑шаблоне не совпадает с фактическим**  
   `PARAMETERS.md` §9 указывает `/home/oem/Desktop/kSystem`, а в audit‑пакете хост – `server`, путь – `/home/jaba/web/ksystem`. Документация не синхронизирована с развёртыванием.

2. **Отсутствие `.gitignore` для временных файлов пайплайна**  
   Правило `.gitignore` описано в `CLAUDE.md` §7, но не проверено. Файлы вроде `define.log`, `_master_progress.json` могут случайно попасть в репозиторий.

3. **Отсутствие версионирования лексиконов**  
   `lexicon.json` перезаписывается на месте. Невозможно воспроизвести исследование по состоянию на определённую дату без ручного просмотра истории git.

4. **Несоответствие порядка IPA‑fallback**  
   `CLAUDE.md` §4.5 предписывает `espeak-ng → epitran → eng-to-ipa → pypinyin`, но в `PARAMETERS.md` §2 `eng-to-ipa` указан для `en` только после `epitran`. Документация противоречит сама себе.

5. **Отсутствие поддержки RTL в полной мере**  
   `DESIGN.md` упоминает RTL для арабского, но нет доказательств, что интерфейс корректно отрабатывает bidirectional text для всех панелей.

---

## STRENGTHS

- **Глубокая теоретическая проработка**: формальные аксиомы, трёхслойная архитектура, Syncordia с чёткими определениями – редкое качество в прикладных проектах.
- **Согласованная система документации**: 9 core‑файлов взаимно ссылаются, не дублируют информацию, образуют замкнутый контур описания.  
- **Чёткие контракты данных**: schema lexicon entry, bible verse, syncordia rule – документированы и стабильны.  
- **Принцип статичности и отсутствие CDN‑зависимостей**: обеспечивает долговременную работоспособность и независимость от внешних сервисов.

---

## ROOT CAUSES

Основная причина большинства проблем – **недостаток инженерной дисциплины на этапе перехода от прототипа к продукту**. Проект развивался как исследовательский (bus factor = 1, нет тестов, кодовая база собиралась экспериментально). Заявленные архитектурные правила либо не были согласованы с реальностью (Rust+Phoenix), либо не были внедрены (Rust‑сервер без документации). Отсутствие автоматической проверки (тесты, CI, schema validation) позволяет регрессиям оставаться незамеченными. Высокая когнитивная нагрузка на единственного разработчика привела к расхождению кода и документации.

---

**Итог**: проект требует существенной доработки перед публичным запуском. Рекомендуется пересмотреть архитектуру, устранить Rust‑сервер либо задокументировать его, внедрить тестирование, выделить модули во фронтенде и синхронизировать документацию с кодом. MAJOR_REVISION.

# Last plan

# План улучшений: konkretnye, izmerimye shagi (actionable steps)

## P0 — Блокеры (must fix before launch)

### P0.1 — Привести стек в соответствие правилу: frontend → Phoenix LiveView, backend → Rust
**Что сделать:**  
- Переписать все UI-компоненты (поиск, SRS, навигация, синкордия) на Phoenix LiveView с сохранением static-first? (LiveView требует сервера, поэтому отказ от static-first – это architectural change. Нужно согласовать: либо переопределить принцип "static-first" для production, либо оставить JS и обновить правило).  
**Затронутые файлы:** `website/js/app.js`, `website/index.html`, `website/css/style.css` → перенос в Phoenix шаблоны и присоединение к серверу.  
**Оценка:** L (недели разработки)  
**Риск:** высокий – требует изучения Phoenix LiveView, переработка всей логики, нарушение static-first.

### P0.2 — Решить судьбу Rust-сервера (`server/`)
**Что сделать:**  
- Если сервер используется (например, для генерации данных или API), задокументировать его роль в DESIGN.md (новый раздел "Backend API") и описать контракты.  
- Если не используется – удалить (`git rm -r server/`) или переместить в `_archive/`.  
**Затронутые файлы:** `server/Cargo.toml`, `server/Dockerfile`, `server/src/*`, `DESIGN.md`.  
**Оценка:** S (1–2 часа)  
**Риск:** низкий – просто документирование/удаление.

### P0.3 — Внедрить автоматические тесты (unit + schema + integration)
**Что сделать:**  
- Добавить Rust-тесты для любого backend-кода (если есть) и Python-тесты для пайплайна (pytest).  
- Написать schema-валидацию для `lexicon.json` и `bible.json` (JSON Schema + assert).  
- Добавить CI (GitHub Actions) для прогона тестов при каждом push.  
**Затронутые файлы:** `server/Cargo.toml` (добавить `[dev-dependencies]`), новые `tests/` директории, `pyproject.toml` (зависимости), `.github/workflows/ci.yml`.  
**Оценка:** M (несколько дней)  
**Риск:** низкий – повышает надёжность, не меняет логику.

### P0.4 — Оптимизировать производительность фронтенда (ленивая загрузка + индексы)
**Что сделать:**  
- Разбить лексиконы на чанки (по языку или по алфавиту) и загружать только при открытии соответствующего языка/поиске.  
- Добавить префиксный trie (в виде JSON) для быстрого поиска по началу слова, альтернатива – бинарный поиск по отсортированному массиву.  
- Ограничить объём загружаемых данных на старте: загружать только мета-информацию (`allLexMini`), полные записи – по требованию.  
**Затронутые файлы:** `website/js/app.js`, `website/data/` (сгенерировать индексы через скрипты).  
**Оценка:** M (3–5 дней)  
**Риск:** средний – может потребовать изменений в пайплайне генерации данных.

### P0.5 — Декомпозировать монолитный `app.js` на модули
**Что сделать:**  
- Выделить логику SRS в отдельный файл `srs.js`.  
- Выделить логику Syncordia (если будет) в `syncordia.js`.  
- Выделить поисковый движок в `search.js`.  
- Использовать ES modules (type="module") или самодельную простую модульную систему (без бандлера).  
**Затронутые файлы:** `website/js/app.js` → разделить на `srs.js`, `syncordia.js`, `search.js`, `navigation.js`, `settings.js`, оставить `app.js` как главный инициализатор.  
**Оценка:** M (2–3 дня)  
**Риск:** низкий – чисто рефакторинг, но требует осторожности с глобальными переменными.

### P0.6 — Обеспечить версионирование данных и воспроизводимость
**Что сделать:**  
- При каждой регенерации `lexicon.json` создавать файл с меткой времени или тегом git (например, `lexicon-2026-05-08.json`).  
- Хранить все версии в отдельной директории `website/data/versions/`.  
- В `app.js` загружать последнюю версию по умолчанию, с возможностью выбора предыдущей через URL-параметр.  
**Затронутые файлы:** `build_lexicons.py`, `enrich_lexicons.py`, `website/js/app.js`, `DESIGN.md` (дополнить контракт).  
**Оценка:** M (2–3 дня)  
**Риск:** низкий – не влияет на существующую логику, добавляет возможность отката.

---

## P1 — Важно (сделать в ближайшие спринты)

### P1.1 — Переместить Python-скрипты в `scripts/` и обновить пути
**Что сделать:**  
- `git mv build_lexicons.py enrich_lexicons.py fetch_bibles.py fetch_georgian_arabic.py convert.py scripts/`  
- Обновить все ссылки в `README.md`, `PARAMETERS.md`, `CLAUDE.md`.  
**Затронутые файлы:** все `.py` в корне → `scripts/*.py`, документация.  
**Оценка:** S (1 час)  
**Риск:** низкий.

### P1.2 — Синхронизировать пути и параметры между документацией и реальностью
**Что сделать:**  
- Исправить `PARAMETERS.md` §9: `WorkingDirectory` → `/home/jaba/web/ksystem`.  
- Проверить все пути в `CLAUDE.md` и `DESIGN.md` на соответствие текущей структуре.  
- Добавить проверку в CI: `for f in $(grep -r '/home/oem' .); do ...`  
**Затронутые файлы:** `PARAMETERS.md`, `CLAUDE.md`, `DESIGN.md`.  
**Оценка:** S (30 минут)  
**Риск:** низкий.

### P1.3 — Добавить `.gitignore` и запретить случайный push временных файлов
**Что сделать:**  
- Создать `.gitignore` с: `define.log`, `_master_progress.json`, `_progress.json`, `*.pyc`, `__pycache__/`, `.env`, `venv/`.  
- Добавить pre-commit hook (через `pre-commit`) для проверки отсутствия больших JSON и логов.  
**Затронутые файлы:** `.gitignore` (новый), `.pre-commit-config.yaml` (опционально).  
**Оценка:** S (30 минут)  
**Риск:** низкий.

### P1.4 — Исправить несоответствие порядка IPA fallback в документации
**Что сделать:**  
- В `PARAMETERS.md` §2 привести порядок и языки в соответствие с `CLAUDE.md` §4.5 (espeak-ng → epitran → eng-to-ipa → pypinyin).  
- Убедиться, что `build_lexicons.py` действительно использует именно этот порядок.  
**Затронутые файлы:** `PARAMETERS.md`, `build_lexicons.py`.  
**Оценка:** S (30 минут)  
**Риск:** низкий.

### P1.5 — Улучшить поддержку RTL (правописание) для арабского
**Что сделать:**  
- Добавить CSS-класс `.rtl` для контейнеров арабского языка.  
- Проверить, что все панели (поиск, деталь, SRS) корректно отображаются при смене направления.  
- Добавить `dir="auto"` для текстовых узлов.  
**Затронутые файлы:** `website/css/style.css`, `website/js/app.js` (при смене языка менять `dir` на `body`).  
**Оценка:** S (2–3 часа)  
**Риск:** низкий.

---

## P2 — Nice-to-have (после релиза)

### P2.1 — Модуль «Syncordia»: реализовать правила tabu/reward/measure в коде
**Что сделать:**  
- Создать `syncordia.js` с функцией `evaluate(action) → {verdict, points}`.  
- Интегрировать в `app.js` при кликах по словам (вызов `evaluate`).  
- Добавить отображение очков и уведомлений.  
**Затронутые файлы:** `website/js/syncordia.js` (новый), `website/js/app.js`, `website/data/syncordia.json`.  
**Оценка:** M (3–5 дней)  
**Риск:** средний – может замедлить UX, если не продумать кэширование.

### P2.2 — Добавить schema validation в CI для всех JSON-файлов
**Что сделать:**  
- Написать JSON Schema для `lexicon.json`, `bible.json`, `geo.json`, `syncordia.json`.  
- Добавить шаг в CI (python `jsonschema` или `ajv`) для проверки.  
**Затронутые файлы:** `.github/workflows/ci.yml`, `schemas/` (новая директория), `pyproject.toml`.  
**Оценка:** S (1–2 дня)  
**Риск:** низкий.

### P2.3 — Внедрить ленивую загрузку лексиконов (Streaming)
**Что сделать:**  
- Использовать `fetch()` с `Range` или разбить JSON на несколько файлов по range (например, по буквам).  
- Загружать только нужный язык при активации.  
**Затронутые файлы:** `website/js/app.js`, `server/` (если сервер будет отдавать частями), либо статический JSON разбить скриптом.  
**Оценка:** L (неделя)  
**Риск:** средний – требуется рефакторинг загрузки данных.

### P2.4 — Покрыть тестами IPA-цепочку и enrichment (детерминизм)
**Что сделать:**  
- Написать pytest-тесты, которые прогоняют `get_ipa` на заранее известных эталонах (например, для английского "love" → /lʌv/).  
- Тест enrich: проверить, что `kin` и `cat` назначаются согласно правилам на тестовом корпусе.  
**Затронутые файлы:** `tests/test_ipa.py`, `tests/test_enrich.py`, `build_lexicons.py` (рефакторинг для тестируемости).  
**Оценка:** M (2–3 дня)  
**Риск:** низкий.

---

## Сводка по P0 с оценкой трудоёмкости и риска

| ID | Шаг | Трудоёмкость | Риск |
|----|-----|--------------|------|
| P0.1 | Миграция на Phoenix LiveView (frontend) + Rust (backend) | L | Высокий |
| P0.2 | Решить судьбу существующего Rust-сервера (удалить/документировать) | S | Низкий |
| P0.3 | Внедрить тесты + CI | M | Низкий |
| P0.4 | Оптимизация производительности фронтенда (ленивая загрузка, индексы) | M | Средний |
| P0.5 | Декомпозиция app.js на модули | M | Низкий |
| P0.6 | Версионирование данных | M | Низкий |

**Примечание:** Если правило стека (Rust+Phoenix LiveView) является строгим требованием, P0.1 имеет наивысший приоритет и самый большой риск. Альтернатива – переопределить правило в документации (обновить CLAUDE.md §2), что является P1-шагом, но устранит блокер. Рекомендуется сначала обсудить с автором, примет ли он изменения правила, иначе проект будет остановлен на неопределённый срок.

# Last check feedback

## VERDICT
NEEDS_REVISION

## REMAINING_GAPS
1. **P0.1 (стек — Phoenix LiveView vs. static-first) не закрыт.** План предлагает два варианта (миграция на LiveView или снятие требования), но не фиксирует выбор. Без чёткого решения этот блокер остаётся неразрешённым и блокирует следующие шаги.
2. **Правило языка (CLAUDE.md §2) нарушается** — план предлагает Elixir/Phoenix без обоснования, почему Rust не подходит для фронтенда. Необходимо либо обновить правило, либо привести аргументацию.
3. **Не адресованы критические проблемы из OPEN_PROBLEMS.md:** LLM-определения без верификации (§1.1), одноклассификаторная классификация без Cohen’s κ (§1.2), отсутствие тестов для IPA и enrichment (это частично покрыто P2.4, но не как P0). Хотя формально эти проблемы могут не быть «блокерами», план их игнорирует, что может быть сочтено критическим для научной достоверности.

## NOTES
- План хорошо структурирован по приоритетам и даёт конкретные действия.
- Оценка трудоёмкости и риска полезна, но для P0.1 риск «высокий» — это сигнал, что решение нужно принимать до реализации.
- В плане есть мелкие опечатки (konkretnye, izmerimye, shagi) — рекомендуется исправить.
- Рекомендуется доработать план: сначала принять архитектурное решение по стеку (или явно утвердить текущий static-first + vanilla JS, обновив CLAUDE.md), затем включить в P0 верификацию хотя бы одного языка (sample audit из OPEN_PROBLEMS §1.1) как критическую для научной базы.

## Instruction
Переработай план так, чтобы закрыть REMAINING_GAPS из последней проверки.