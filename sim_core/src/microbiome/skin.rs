/// Кожный микробиом
/// Влияет на L_epidermal, барьерную функцию
/// В v1.0: заглушка. Реализация — Phase 3.
pub struct SkinMicrobiome {
    pub diversity_index: f64,
    pub staphylococcus_ratio: f64,
}
