[package]
name = "aim-ai-fix-planner"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AIM AI — fix planner (S14). Group shared findings by file, read cited lines, emit a punch-list with heuristic recommendations. Rust port of AI/ai/fix_planner.py."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
chrono = { workspace = true }
regex = "1"
once_cell = "1"

[dev-dependencies]
tempfile = "3"
