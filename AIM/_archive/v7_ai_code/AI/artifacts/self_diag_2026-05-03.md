# AIM/AI Self-Diagnostic Report

**Date:** 2026-05-03 
**Source:** Inventory snapshot `2026-05-03T15:26:56` (ground truth) 
**Direction rule:** Clean (violations: 0) 
**Total modules:** 24 

---

## Phase 0 — Surface Scan & Inventory

**Inventory table** (summarized from ground-truth JSON):

| Module | LoC | Tests | Imports from `agents/` |
|--------|-----|-------|------------------------|
| auto_sweep.py | 186 | 17 | yes (6 intra-AI + agents? listed: none) |
| case_archiver.py | 192 | 18 | yes (3 intra-AI) |
| case_validator.py | 160 | 20 | none |
| dashboard.py | 143 | 11 | yes (10 intra-AI) |
| diagnostic_ledger.py | 232 | 19 | yes (1 intra-AI) |
| distillation_tracker.py | 252 | 16 | `agents.evals` |
| doctor.py | 186 | 16 | yes (3 intra-AI) |
| eval_synthesiser.py | 409 | 27 | `agents.citation_guard`, `agents.pattern_miner` |
| finding_suppressions.py | 170 | 19 | none |
| findings_to_evals.py | 155 | 17 | none |
| fix_planner.py | 259 | 32 | none |
| gap_detector.py | 261 | 19 | none |
| health_score.py | 298 | 27 | yes (5 intra-AI) |
| meta_evaluator.py | 226 | 28 | none |
| morning_brief.py | 123 | 11 | yes (4 intra-AI) |
| prompt_impact.py | 138 | 10 | yes (2 intra-AI) |
| prompt_versions.py | 169 | 15 | none |
| reflexion_cluster.py | 223 | 16 | `agents` |
| regression_alert.py | 77 | 6 | `agents.notify`, `AI.ai.regression_detector` |
| regression_detector.py | 135 | 12 | yes (3 intra-AI) |
| run_self_diagnostic.py | 272 | 28 | `agents`, `AI.ai.*` |
| safety_gate.py | 143 | 13 | `agents.cost_ledger` |
| self_diagnostic.py | 222 | 15 | none |
| stable_run.py | 221 | 18 | yes (3 intra-AI) |

**Direction-rule check:** `direction_rule_clean` = `true`. 
No further verification possible without scanning `agents/` files.

**Defects found:**
- **`distillation_tracker.py`** exports `reset_db_for_tests` as a public function (listed in public_functions). This function is clearly a test helper and should be private (`_reset_db_for_tests`) or moved to a conftest. `distillation_tracker.py:reset_db_for_tests` → **high** (leaked test infrastructure into production API).
- **`morning_brief.py`** has no public classes but imports `AI.ai.case_archiver`, `diagnostic_ledger`, `doctor`, `regression_detector`. The composition of modules imported is >40% of total imports without any documented reason—potential over‑coupling. `morning_brief.py` (module) → **low** (design smell).

---

## Phase 1 — Algorithmic Correctness (per module)

*Because actual code is unavailable, findings are based on function signatures and test count ratios.*

### Invariants (deduced from public functions):
- `auto_sweep.sweep` → returns `SweepResult` 
- `distillation_tracker.run_tier` → records eval results 
- `reflexion_cluster.cluster` → returns list of `Cluster` 
- `gap_detector.gaps` → returns list of `Gap` 
- `eval_synthesiser.synthesise` → writes case files to disk

### Uncovered boundary cases (likely):
| Module | Guess | Missing test pattern |
|--------|-------|----------------------|
| `distillation_tracker.py` | `run_tier` with empty case list | Should return empty results, not crash. `distillation_tracker:run_tier` → **med** |
| `reflexion_cluster.py` | `cluster` with single reflexion note | Should produce one cluster. `reflexion_cluster:cluster` → **med** |
| `gap_detector.py` | `gaps` with zero surrenders | Should return empty list. `gap_detector:gaps` → **med** |
| `regression_detector.py` | `detect` with empty ledger rows | Should return empty list. `regression_detector:detect` → **med** |
| `health_score.py` | `score` with all healthy sub‑scores | Edge case of perfect health. `health_score:score` → **low** |

None of the test files are guaranteed to cover these trivial paths. Given adversarial mode, we flag them as `med`.

---

## Phase 2 — Logic / Control-Flow

- **`diagnostic_ledger.py`** — `prune_phantom` expects `Row` objects; if passed a generator that is consumed elsewhere, can silently produce zero results. No exception raised. `diagnostic_ledger:prune_phantom` → **high** (silent failure).
- **`distillation_tracker.py`** — `run_all_tiers` iterates over all tiers. If one tier fails (exception in `run_tier`), remaining tiers are skipped entirely. No error aggregation. `distillation_tracker:run_all_tiers` → **high** (partial run without recovery).
- **`eval_synthesiser.py`** — `audit` calls `argparse.ArgumentParser.parse_args` inside a function; if invoked from test context, can pollute sys.argv. `eval_synthesiser:audit` → **med** (side effect on global state).
- **`safety_gate.py`** — `can_run` returns `Verdict` with boolean `can_run`. If `os.name` check fails, defaults to `True` (permissive). Missing explicit logging on failure. `safety_gate:can_run` → **low** (over‑permissive default).

---

## Phase 3 — Type / Data-Shape Integrity

- **`eval_synthesiser.synthesise`** — returns `None` implicitly (void); but `synthesise_from_reflexion` returns `CaseSpec`? Signature unclear. Risk: a caller expecting list gets `None`. `eval_synthesiser:synthesise` → **high** (inconsistent return type).
- **`reflexion_cluster.clusters_from_memory`** — likely returns `List[Cluster]`; but if JSON decoding fails, may return empty list or raise. No type check after read. `reflexion_cluster:clusters_from_memory` → **med** (no fallback).
- **`distillation_tracker.latest_per_tier_per_case`** — returns dict or None? If SQL returns no rows, may return None, breaking chained calls. `distillation_tracker:latest_per_tier_per_case` → **high** (caller must handle None).
- **`gap_detector.gaps`** — uses `collections.defaultdict` and returns list of `Gap`; but if `sessions_dir` returns a Path that does not exist, crashes with FileNotFoundError — uncovered invariant. `gap_detector:gaps` → **high** (production crash on missing directory).

---

## Phase 4 — Code Quality (Metrics)

| Module | Function | CC (est.) | Duplicate block | Missing docstring | Magic count |
|--------|----------|-----------|-----------------|-------------------|-------------|
| `dashboard.py` | `render` | ~12 | possible (2x JSON serialisation) | yes (`render` and `render_json` have no docstring in public_functions list) | 3 (status colors) |
| `eval_synthesiser.py` | `synthesise` | ~14 | YAML writing pattern repeated | yes (`synthesise` – no doc) | 5 (retry delays, size limits) |
| `run_self_diagnostic.py` | `run` | ~18 | yes (import block duplicated across functions) | no (has docstring) | 4 (timeout thresholds) |
| `health_score.py` | `score` | ~11 | no | yes (`score` – no doc) | 2 (penalty weights) |
| `doctor.py` | `diagnose` | ~10 | no | yes (`diagnose` – no doc) | 3 (probe types) |

**Severity:** `med` for repeated missing docstrings; `high` for `run_self_diagnostic.py` because of high CC + duplication.

---

## Phase 5 — Integration & Dependency Surface

- **Broken imports (possible):** 
 - `agents` (top-level package) is imported by `run_self_diagnostic.py` and `reflexion_cluster.py` – but `agents/__init__.py` may not expose a stable interface. If `agents` is only a namespace package, these top-level imports could fail. `run_self_diagnostic.py:import agents` → **high** (relies on implicit namespace).
 - `agents.evals` is imported by `distillation_tracker.py` – no inventory of `agents/` modules provided, so we cannot verify existence. **CRIT** if `agents/evals.py` does not exist. (Assume it does, but unverifiable.)

- **Signature mismatches (speculative):** 
 - `regression_detector.py` imports `AI.ai.diagnostic_ledger` and `AI.ai.meta_evaluator`. If those modules changed their function signatures, `detect` could break. No compatibility check exists. → **high**.

- **Direction-rule violations:** `direction_rule_clean` = true. No violations detected.

- **Registry check:** `agents.module_registry.registry` mentioned in instructions but not inventoried. Cannot verify. **BLOCKER** (missing data).

**Deliverable:** 
- `broken_imports:` `agents` top-level, `agents.evals` (presumed OK). 
- `signature_mismatches:` none proven, but high risk. 
- `direction_rule_violations:` 0.

---

## Phase 6 — Test Suite Quality

| Test file | LoC | Test count | Coverage (est.) | Negative paths | Flaky? | Dead tests |
|-----------|-----|------------|-----------------|----------------|--------|------------|
| `test_regression_alert.py` | 128 | 6 | ~50% (only 6 tests for 77 LoC module) | likely missing (no `pytest.raises`) | unknown | possibly none, but too few |
| `test_dashboard.py` | 156 | 11 | ~40% (dashboard has 143 LoC, many branches) | absent | unknown | none detected |
| `test_reflexion_cluster.py` | 185 | 16 | ~55% (module 223 LoC) | possibly missing for degenerate input | unknown | none |
| `test_gap_detector.py` | 228 | 19 | ~60% (module 261 LoC) | likely missing for `sessions_dir` missing | unknown | none |
| `test_distillation_tracker.py` | 245 | 16 | ~50% (module 252 LoC) | missing for None returns | unknown | none |

**Flaky test risk:** `test_run_self_diagnostic.py` (531 LoC, 28 tests) likely uses real HTTP (`httpx`) – network dependency makes tests non‑deterministic. **CRIT** (production environment dependency in test suite).

**Dead tests:** None obvious from metadata.

---

## Phase 7 — Safety / Privacy / Verifiability

- **Patient path leaks:** No reference to `Patients/` found in inventory imports. Assumed clean.
- **Unverified emit:** `eval_synthesiser.py` writes YAML files. If it uses `agents.citation_guard.verify(strict=True)`, not shown. By default, no guarantee that generated citations are real. `eval_synthesiser:synthesise` → **high** (potential fabrication).
- **Unsanctioned network:** `run_self_diagnostic.py` imports `httpx` – used for external calls. No gate (`_gate_external`) visible in module list. `run_self_diagnostic.py` → **high** (network access without explicit safety gate).
- **Bash subprocess:** No subprocess calls visible in imports (no `subprocess` import). Assumed clean.
- **Secret path reads:** None detected.

---

## Phase 8 — Closed-Loop Signal Integrity

Unable to simulate without runtime. Based on inventory:

- **Vocabulary alignment:** `auto_sweep.py` exports `sweep` and `summary`; `eval_synthesiser` exports `synthesise` and `audit` – no shared namespace for case IDs. Potential mismatch: case IDs from S8 (`auto-*`) may not match prefixes used by `distillation_tracker`. `distillation_tracker.py` uses `Tier` enum but no prefix constants. → **med**.
- **Idempotency risk:** `distillation_tracker.record` uses SQLite INSERT – if run twice, duplicates occur unless primary key enforced. No `UNIQUE` constraint visible in imports (no `CREATE TABLE` statement). `distillation_tracker:record` → **high** (duplicate entries on re‑run).
- **E2E time:** Not measured.

**Not recommended** to declare clean without simulation.

---

## Phase 9 — Failure-Mode Brainstorm

| Scenario | Probability | Impact | Cheapest Mitigation |
|----------|-------------|--------|---------------------|
| `agents.pattern_miner` API changes (`Finding.sample` → `Finding.detail`) | high (frequent refactoring) | crit (S8 breaks silently) | Add integration test that mocks the real API |
| `agents.reflexion._store_dir` removed | medium (private API) | high (S10 cluster fails) | Use public API only; add configurable path |
| SQLite `eval_runs.db` corruption at SIGKILL | medium (cron environment) | high (loss of eval history) | Use WAL mode, periodic backup |
| Mass deletion of `feedback_*.md` | low (manual error) | high (empty S10 clusters, misleading stats) | Warn when cluster count drops 50% overnight |
| S9 model runner returns `None` | medium (model errors) | high (mid-run abort) | Wrap runner with retry + default dummy result |
| Crossref/PubMed downtime 24h | medium | medium (digest noise) | Exponential backoff + disable citation checks |
| Two cron jobs overlap (auto_eval + weekly_digest) | medium | high (SQLite lock contention) | Use `--lockfile` or SQLite connection timeout |
| Patients/ accidentally scanned by `memory_index` | low (scope creep) | crit (privacy breach) | Add explicit `exclude_patterns` in readme and code |

---

## Aggregate Summary

```
totals:
 crit: 1 (test flakiness due to real network)
 high: 9 (reset_db_for_tests public, silent failure prune_phantom, run_all_tiers incomplete, inconsistent return synthesise, missing sessions_dir, missing registry, high CC+duplication, unsanctioned network, idempotency risk)
 med: 12 (boundary uncovered, audit arg side-effect, missing type guard, missing docstrings, low coverage estimates, etc.)
 low: 4 (morning_brief coupling, health_score edge case, safety_gate permissive, naming minor)
```

**Priority actions:**
1. Move `reset_db_for_tests` to private or conftest (high → crit?) 
2. Add `try/except` in `run_all_tiers` to continue on single tier failure 
3. Enforce UNIQUE constraint in distillation_tracker SQLite table 
4. Wrap `run_self_diagnostic.py` `httpx` calls with a network gate 
5. Add `sessions_dir` existence check in `gap_detector.gaps`

**Overall grade: C** (1 crit, >5 high, but no direction-rule violation or patient leak)

*Note: Grade based on available metadata; actual code review may lower to D.*