[package]
name = "aim-ai-eval-synthesiser"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AIM AI — synthesise YAML eval cases from observed failures (S8). Reflexion notes + pattern findings → regression eval cases. Rust port of AI/ai/eval_synthesiser.py."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
regex = "1"
once_cell = "1"
sha2 = "0.10"
hex = "0.4"
chrono = { workspace = true }

[dev-dependencies]
tempfile = "3"
