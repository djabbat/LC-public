[package]
name = "aim-ai-self-diagnostic"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AIM AI — self-diagnostic prompt builder (SD1). Snapshot AI/ai/* inventory and fold it into SELF_DIAGNOSTIC_PROMPT.md so the model can't fabricate the surface it audits. Rust port of AI/ai/self_diagnostic.py."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
regex = "1"
once_cell = "1"
chrono = { workspace = true }

[dev-dependencies]
tempfile = "3"
