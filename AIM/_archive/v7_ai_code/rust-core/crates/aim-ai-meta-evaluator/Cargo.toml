[package]
name = "aim-ai-meta-evaluator"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AIM AI — diagnostic reproducibility metrics (S12). Parse audit reports, measure grade variance + Jaccard + signal/noise across N runs. Rust port of AI/ai/meta_evaluator.py."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
regex = "1"
once_cell = "1"

[dev-dependencies]
tempfile = "3"
