# Place at /etc/app-social/.env (mode 0600, owned by root:jaba)
#
# DATABASE_URL must match the one in /etc/app-realtime/.env (same Postgres
# database/role; the social-server and realtime app share state).

DATABASE_URL=postgres://lcommon:<POSTGRES_PASSWORD>@127.0.0.1:5432/longevitycommon
